---
title: Releasing
description: How a release is cut, built, verified, and published, what to do when one fails partway, and which credential the npm publish uses. Read before tagging a release or editing the release workflows.
tags: [release, ci, npm, cargo-dist]
---

# Releasing

<!-- refs -->
@ref[.github/workflows/release.yml as ReleaseWorkflow]
@ref[.github/workflows/publish-npm.yml as PublishWorkflow]
@ref[dist-workspace.toml as DistConfig]
@ref[docs/DISTRIBUTION.md as Distribution]

## 1. Cutting a release
<!-- @anchor[dist/release-procedure] -->

The version in @ref[Cargo.toml] (`[workspace.package] version`) is the source of truth; dist
refuses a tag that disagrees with it.

1. Bump the version and add a @ref[CHANGELOG.md] entry in a PR; merge to `main`.
2. Rehearse with a prerelease tag, `v<version>-rc.N`. It runs the whole pipeline for real: real
   archives, a real GitHub prerelease, real npm packages published under the `next` dist-tag so
   they never claim `latest`.
3. Tag `v<version>` on `main` and push it. Tags are pushed by hand today; releasing on a version
   bump is tracked in @ref[TODO.md].

## 2. What runs

@[ReleaseWorkflow] is generated by cargo-dist from @[DistConfig] and runs on any tag that looks
like a version. Jobs, in order:

| Job | Does |
|---|---|
| `plan` | validates the tag against the version and lists every artifact; it, `host`, `announce`, and the global build run on the `global` runner pinned in @[DistConfig] |
| `build-local-artifacts` | one runner per target, pinned in @[DistConfig] to current runner images |
| `build-global-artifacts` | installer scripts and the manifest |
| `host` | uploads and creates the GitHub Release. **From here the release is public.** |
| `custom-publish-npm` | calls @[PublishWorkflow]; skipped for prereleases unless `publish-prereleases` is on (it is) |
| `announce` | a barrier, green only if everything landed |

@[PublishWorkflow] is three jobs rather than one, so a platform that cannot run its own binary
blocks all six publishes instead of being discovered afterwards:

| Job | Does |
|---|---|
| `build` | @ref[scripts/src/npm/build-packages.mjs] assembles the five platform packages and the shim from the manifest and archives, packs them, and writes the index (@ref[scripts/src/npm/packages-index.mjs]). The packed tarballs travel between jobs, not the directory tree: artifact zips drop the executable bit, and a mode lost in transit reaches users as `EACCES`. |
| `verify` | one real runner per platform package (macOS arm64 and Intel, Linux x64 and arm64, Windows), `fail-fast: false`; @ref[scripts/src/npm/verify-packages.mjs] installs the tarballs the way a registry install would and runs the binary on a broken and a clean fixture |
| `publish` | needs both, with no `if:` (the generated workflow uses `always()` liberally and copying it here would disable the gate); @ref[scripts/src/npm/publish-packages.mjs] publishes the platform packages first and the shim last, skipping any version already on the registry |

Shim last matters: `context-anchors` is the only package anyone installs, so while it is
unpublished a partial run is invisible to users. The orchestration lives in scripts rather than in
workflow YAML because npm reads a bare `owner/repo`-shaped argument as a GitHub shorthand rather
than a directory, which is the bug that stopped the 0.0.1 shim publishing, and because
verification has to run on Windows too.

## 3. When it fails

- **A verify failure leaves a public GitHub Release with no npm packages.** `host` runs before any
  publishing. That is acceptable: the installers work, and npm is filled in by a re-run.
- **Re-run all jobs, never just the failed one.** A new attempt clears the run's artifacts, so the
  publish job alone would find neither the tarballs nor the archives. Re-running everything is
  safe because publishing skips versions already on the registry.
- **A re-run cannot pick up a fix.** It replays the workflow as of the tagged commit. npm versions
  are consumed permanently, so a fix means a new version, not a retry. 0.0.1 proved it: five
  platform packages published and the shim did not, and the recovery was 0.0.2. The orphaned 0.0.1
  packages are deprecated.
- **Every ambiguous registry probe publishes.** Publishing something already published is a
  harmless 403; skipping something that is not published ships a shim whose optional dependency
  404s, which npm swallows silently and no later run would notice.
- **An authentication failure consumes nothing.** If the first `npm publish` is refused, the
  trusted publisher on npm is misconfigured (wrong workflow filename, `npm publish` not allowed);
  fix it there and re-run all jobs. No version was spent.

## 4. Credentials
<!-- @anchor[dist/publishing-credentials] -->

There is no registry token. Each of the six packages has a trusted publisher, configured in its
settings on the npm website, naming this repository and @[ReleaseWorkflow]: npm validates the
*calling* workflow, not the reusable @[PublishWorkflow] that holds `npm publish`. The publish job
exchanges its OIDC token (`id-token: write`, granted in both workflows) for a credential that lives
as long as the run, and provenance is attached without `--provenance`. The repository URL that
@ref[scripts/src/npm/build-packages.mjs] writes into every manifest must keep matching the GitHub
repository, or npm refuses the publish.

Trusted publishing needs npm 11.5.1 or later, which is why @[PublishWorkflow] runs Node 24. Each
publisher allows direct `npm publish`; npm's default for new publishers is staged publishing, where
CI stages and a maintainer approves each package with 2FA, and switching to it is a pipeline change
tracked in @ref[TODO.md].

A trusted publisher can only be configured on a package that already exists, so a *new* package
needs a one-time bootstrap: publish it once with a granular automation token, configure its
publisher, revoke the token. That is how all six were created at 0.0.1.

## 5. Editing the pipeline

- Any change to @[DistConfig] needs `dist generate` to refresh @[ReleaseWorkflow], or `plan` fails
  its stale-CI check. Do not hand-edit the generated workflow.
- @[PublishWorkflow] is ours and is not regenerated. Its `concurrency` group must keep
  `cancel-in-progress: false`; cancelling partway through the publish loop is how a partial
  publish gets made.
- The verify matrix and the build-target runner map in @[DistConfig] must name the same platforms.
- Every release gets a @ref[CHANGELOG.md] entry. What the artifacts are and where they go is in
  @[Distribution].

## Rejected alternatives

- Tag-triggered releases over release-on-version-bump, for now: a tag pushed with `GITHUB_TOKEN`
  fires nothing, so the bump flow needs cargo-dist's `dispatch-releases` mode, which is queued
  work rather than a reason to keep tagging by hand.
- One publish job over build, verify, publish: a broken platform would be discovered after five
  versions were already spent.
- Shipping the built directory between jobs over packed tarballs: the executable bit does not
  survive the artifact zip.
- `always()` on the publish job: it would run even when verify failed.
